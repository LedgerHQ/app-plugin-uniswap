# This script will build the Ethereum application that are needed for the plugin test setup
# Run the helper script helper_tool_build_dependencies.py after to build and dispatch them in the correct location

from _helper_tool import clone_and_pull_ethereum

clone_and_pull_ethereum()
